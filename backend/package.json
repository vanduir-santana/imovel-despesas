{
  "name": "imovel-despesas-api",
  "version": "1.0.0",
  "description": "API para cálculo de despesas com imóvel. Os gastos retornados pela aplicação são relativos a cartório, banco, etc.",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "dev": "nodemon app.js"
  },
  "author": "Vanduir Santana Medeiros",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "nodemonConfig": {
    "ignore": ["configs/*.json"]
  }
}
